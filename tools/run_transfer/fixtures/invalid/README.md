<!-- Generated by make_fixtures.py. Do not edit. -->
# Invalid run-transfer fixtures

Each fixture is the valid fixture (`../valid/synthetic.run.json`) with exactly one thing
wrong, and it is filed under the layer expected to refuse it. `MANIFEST.json` is the
machine-readable form; the mutations themselves are in `../../make_fixtures.py`.

## `schema/` — 8 fixtures

The JSON Schema refuses these: shape and syntax, before any model is in view.

| fixture | what is wrong | the refusal should say |
|---|---|---|
| `missing-required-field.run.json` | an activity with no localId | plan.activities[0] is missing required property 'localId' |
| `wrong-kind.run.json` | kind is not plandev-run | kind must be 'plandev-run'; refused outright rather than sniffed |
| `unknown-version.run.json` | a major version this reader does not know | version '3' is not supported; this reader accepts version 1 |
| `malformed-value-schema.run.json` | a series ValueSchema with no items | model.resourceTypes[0].schema is missing required property 'items' |
| `wrong-primitive-type.run.json` | results.duration as a string | results.duration must be an integer (microseconds) |
| `calendar-date-start-time.run.json` | results.startTime as a calendar date instead of day-of-year | results.startTime must be uuuu-DDDTHH:MM:SS -- day of year, not a calendar date |
| `null-span-duration.run.json` | an unfinished span written as duration: null rather than omitting the field | results.spans[4].duration must be an integer; omit the field for an unfinished span |
| `unsupported-capability-without-reason.run.json` | a capability declared unsupported with no sentence to show | model.capabilities.simulation is missing required property 'reason' |

## `importer/` — 3 fixtures

The importer refuses these while resolving the file's own cross-references, before anything is written.

| fixture | what is wrong | the refusal should say |
|---|---|---|
| `unknown-directive-local-id.run.json` | a span claiming a directive localId no activity declares | span 1 references directiveLocalId 'a99', which no activity in this file declares |
| `unknown-anchor-local-id.run.json` | an activity anchored to a localId no activity declares | activity 'a2' is anchored to 'a99', which no activity in this file declares |
| `duplicate-local-id.run.json` | two activities sharing one localId | localId 'a1' is declared by more than one activity; it must be unique within the file |

## `gate/` — 11 fixtures

`ExternalResultsGate` refuses these: admissible shape, inadmissible against the declared model. **The schema must ACCEPT every one of them** -- that is the assertion that keeps the two layers genuinely separate.

| fixture | what is wrong | the refusal should say |
|---|---|---|
| `unregistered-resource.run.json` | a profile for a resource the model never declared | resource '/thermal/tempC' is not a registered resource type of this model |
| `resource-schema-drift.run.json` | a resource declared int whose profile is real | resource '/battery/soc' has schema RealSchema[] but is registered as IntSchema[] |
| `unregistered-activity-type.run.json` | a span whose type the model never declared | span 2 has type 'Rewind', which is not a registered activity type |
| `argument-type-mismatch.run.json` | a string where the declared parameter is an int | span 1 (Observe) argument 'priority' is "high" where the schema says int |
| `duplicate-span-id.run.json` | two spans sharing one spanId | duplicate spanId 1 |
| `unresolvable-parent.run.json` | a parentId that is not a span in this result | span 2 has parentId 99, which is not a span in this result |
| `parent-cycle.run.json` | 1 -> 4 -> 3 -> 1 | span 1 is part of a parent cycle |
| `non-finite-real-dynamics.run.json` | a rate that parses to Infinity as a double | resource '/battery/soc' has a non-finite rate (Infinity) |
| `negative-duration.run.json` | a span of negative duration | span 3 has negative duration (-900000000us) |
| `span-outside-window.run.json` | a span that starts inside the simulation and ends after it | span 3 runs to 4400000000us, past the simulation duration of 3600000000us; report it as unfinished (omit `duration`) instead of claiming an end the simulation never reached |
| `activity-type-name-not-ts-identifier.run.json` | a declared activity type whose name cannot be a bare TypeScript identifier | activity type '2Slew' is not a legal TypeScript identifier, so the generated constraint and scheduling typings for this model will not compile |
